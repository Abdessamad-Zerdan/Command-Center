Drop your profile photo here as `profile.jpg` (or update `photo_url` in
`src/command_center/portfolio_data.py` if you'd rather use a different
filename/format). Until the file exists, the hero section falls back to an
"AZ" initials avatar automatically — nothing breaks either way.
